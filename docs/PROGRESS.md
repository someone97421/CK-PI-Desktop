# 进度

> 本 fork 的进度从 `#0` 重新计。上游里程碑（M0–M6）与 BOARD 快照已归档，见
> `archive/project/BOARD.md`。

状态取值：`todo` / `doing` / `done` / `dropped`

| # | 事项 | 状态 | 备注 |
|---:|---|---|---|
| 0 | 建立本 fork 的精简文档与流程（`AGENTS.md` §0、`docs/` 三件套、归档旧文档） | done | 文件已就位；未跑构建/测试（NOT RUN） |
| 1 | pi 配置手动双向同步（决策 `F001`） | todo | 代码已落地，未验证 |

## 记录规则

- 新事项取当前最大编号 +1。
- 未完成的事项保持 `todo` / `doing`，不要提前标 `done`。
- 标 `done` 时在备注里写清验证方式；没跑的写 `NOT RUN` 并说明原因。
