# 上游修复适配记录

本轮以 `vastsa/PI-Desktop` 的 `e60995f7c4f4b926a14807c8c34dfaada71bc030` 为固定快照，
只在本 fork 的当前分支逐项吸收代码及相关测试，不引入上游流程规则或发布工作流。

合并前的品牌隔离、日期版本和图标调整已提交为 `c0861239`。
每批提交独立记录来源，暂不推送。未运行测试、全量构建或应用服务。

## 已适配

| 批次 | 来源提交 | 适配内容与保留项 |
| --- | --- | --- |
| 网络请求 | `10efeb6b`、`f76ec9da`、`300a7c3e`、`bb420327`、`88207665` | 压缩请求带上提供商请求头、网络错误分类、连接恢复、SOCKS5H 映射。冲突中保留 fork 的 Google 适配器禁用自定义 fetch 分支；其余请求路径接入诊断和恢复。 |
| 技能市场网络 | `2fd8dbe1`、`cc4bdac7`、`906f619f`、`2062131f` | 明确展示安装失败与 DNS/策略错误，记录脱敏诊断。按实际代理路由识别 TUN 假 IP；私网、回环及未知路由仍受原安全边界限制。保留 fork 的技能扩展路径 API 和品牌语言。 |
| 插件及委派边界 | `e6779c9f`、`806b1a56`、`e8302cb4`、`418ca3c6`、`aa00bf20` | 协作派生遵守模型委派开关，计划模式明确禁止 Bash 修改工作区；插件文件根目录跟随调用会话，保留错误码，并正确审计失败的网络请求。保留 fork 的指定模型绑定和终端插件。 |
| 聊天显示与阅读 | `6bcccec1`、`dcc3b80f`、`56a90999`、`f4302d91`、`698a506b`、`1cea9db0`、`a8747d60`、`f2ffbedf`、`92668836`、`962db89b`、`8f4d764e`、`f9740689`、`8dab3a04`、`f9bac5a7`、`fe53b277` | 折叠阅读锚点、状态栏稳定占位、工具文件跳转、提示框清理、公式显示、编辑器及问答卡片样式、精确 token 计数。公式分隔符等宽转换，保留本 fork 的批注位置映射；未引入上游删除引用/批注/侧边对话的提交。 |
| 设置菜单与文案 | `58142eb7`、`4720f7f4`、`e56331d5`、`15ded206` | 设置中的紧凑下拉统一为 AnchoredMenu，去掉重复说明。手工解决字体行和八语种冲突：保留分区字体、字重选择/输入、浅深配色、模型导入导出、日期版本与 fork 名称。 |

| 窗口与主题 | `fc45db81`、`05eeaeef`、`b2e07345`、`a295a124`、`7ed2f010`、`87c04e01`、`e131f9c9`、`b96a8ab4`、`b1e56754` | 统一主题颜色令牌、方形图标按钮、窗口控制尺寸和预览面板拖动区域。模型页保留现有提供商操作，未混入数据库归属功能；保留恐龙品牌检查。 |

| 项目与侧边对话 | `1f239de2`、`ccdb0fbc`、`e40d5ed8` | 删除项目时明确提示并由用户确认停止运行任务；侧边对话首次发送才创建会话，发送前检查可用性。保留引用和侧边对话。 |

| 能力管理 | `4533426a`、`404c6590` | MCP/技能支持全局与项目之间移动，增加内置子智能体开关。外部 linked 技能在界面及 host 两端禁止移动，保留扩展目录；开关使用独立 JSON 状态，不改变已有数据库或设置格式。 |

| 插件实时能力 | `d0365381`、`17f94500`、`c9e2c2ec`、`d26867e9` | 接入权限控制的全局快捷键与 WebSocket，补充不支持音频时的明确错误，处理 ws 可选原生依赖打包。保留域名白名单、资源释放及权限撤销边界；仅增加 ws 依赖，不替换 fork 打包身份。 |

| 自定义智能体与插件语言 | `10527245`、`3c9bae9f`、`0fd58300`、`e4ffbfe4` | 可信扩展可注册完整智能体实现，插件行展示智能体名称并随应用语言更新。保留指定模型与 Google fetch 兼容；切换时按当前提供商取凭据，激活失败清理实例，避免复用未激活缓存。移除冲突中夹带的插件提供商数据库特性测试。 |

## 需要单独决定的改动

- 数据库从 16 升至 18，以及依赖该迁移的插件提供商归属、消息优先队列：本轮不升级共享数据库。
- 引用、批注、侧边对话的整组删除（`db001777`）：保留本 fork 现有功能。
- 上游消息“立即发送”的优先块语义：保留 fork 的当前轮次引导及子任务唤醒语义。
- 上游 AGENTS/CLAUDE 镜像、强制 spec/ADR/E2E 流程、发布 CI：继续遵守本 fork 的 `AGENTS.md`。

本轮所有合并都必须保留：中文/英文品牌、小恐龙图标、独立安装身份、日期版本、
fork 更新源、共用 `.pi-desktop`、数据库 schema 16 和 protocol 11、内置终端修复。
