# 上游修复适配记录

第一轮以 `vastsa/PI-Desktop` 的 `e60995f7c4f4b926a14807c8c34dfaada71bc030` 为固定快照，
只在本 fork 的当前分支逐项吸收代码及相关测试，不引入上游流程规则或发布工作流。

合并前的品牌隔离、日期版本和图标调整已提交为 `c0861239`。
第一轮每批提交独立记录来源；这些提交及流程文档已推送至本 fork。未运行测试、全量构建或应用服务。

## 同步状态

- 最后核对的上游 SHA：`206085c07697771f857cab0421bc145e3af41e1d`。
- 最后正式合入的上游 SHA：`206085c07697771f857cab0421bc145e3af41e1d`。
- 对应本地合并提交：见本轮合并后的提交定位记录。
- 合并前本地提交：`c042f5eea8dac4adfb3c90ee40d2614d35f5ce10`，当前分支为 `main`。
- 本轮从实际已合入的 `0111e306c120ad5820688d7608cb37bad8fbcc1f` 增量同步，修正原顶部状态落后于实际合并历史的问题。
- 使用正常 `git merge --no-ff --no-commit` 建立合并关系；本轮仅本地提交，未推送。
- 本轮功能均已吸收或按既定边界适配，无待决定功能项；下次从上述正式合入节点继续。

## 已适配

| 批次 | 来源提交 | 适配内容与保留项 |
| --- | --- | --- |
| 网络请求 | `10efeb6b`、`f76ec9da`、`300a7c3e`、`bb420327`、`88207665` | 压缩请求带上提供商请求头、网络错误分类、连接恢复、SOCKS5H 映射。冲突中保留 fork 的 Google 适配器禁用自定义 fetch 分支；其余请求路径接入诊断和恢复。 |
| 技能市场网络 | `2fd8dbe1`、`cc4bdac7`、`906f619f`、`2062131f` | 明确展示安装失败与 DNS/策略错误，记录脱敏诊断。按实际代理路由识别 TUN 假 IP；私网、回环及未知路由仍受原安全边界限制。保留 fork 的技能扩展路径 API 和品牌语言。 |
| 插件及委派边界 | `e6779c9f`、`806b1a56`、`e8302cb4`、`418ca3c6`、`aa00bf20` | 协作派生遵守模型委派开关，计划模式明确禁止 Bash 修改工作区；插件文件根目录跟随调用会话，保留错误码，并正确审计失败的网络请求。保留 fork 的指定模型绑定和终端插件。 |
| 聊天显示与阅读 | `6bcccec1`、`dcc3b80f`、`56a90999`、`f4302d91`、`698a506b`、`1cea9db0`、`a8747d60`、`f2ffbedf`、`92668836`、`962db89b`、`8f4d764e`、`f9740689`、`8dab3a04`、`f9bac5a7`、`fe53b277` | 折叠阅读锚点、状态栏稳定占位、工具文件跳转、提示框清理、公式显示、编辑器及问答卡片样式、精确 token 计数。公式分隔符等宽转换，保留本 fork 的批注位置映射；未引入上游删除引用/批注/侧边对话的提交。 |
| 设置菜单与文案 | `58142eb7`、`4720f7f4`、`e56331d5`、`15ded206` | 设置中的紧凑下拉统一为 AnchoredMenu，去掉重复说明。手工解决字体行和八语种冲突：保留分区字体、字重选择/输入、浅深配色、模型导入导出、日期版本与 fork 名称。 |
| 窗口与主题 | `fc45db81`、`05eeaeef`、`b2e07345`、`a295a124`、`7ed2f010`、`87c04e01`、`e131f9c9`、`b96a8ab4`、`b1e56754` | 统一主题颜色令牌、方形图标按钮、窗口控制尺寸和预览面板拖动区域。模型页保留现有提供商操作，未混入数据库归属功能；保留恐龙品牌检查。 |
| 项目与侧边对话 | `1f239de2`、`ccdb0fbc`、`e40d5ed8` | 删除项目时明确提示并由用户确认停止运行任务；侧边对话首次发送才创建会话，发送前检查可用性。保留引用和侧边对话。 |
| 能力管理 | `4533426a`、`404c6590` | MCP/技能支持全局与项目之间移动，增加内置子智能体开关。外部 linked 技能在界面及 host 两端禁止移动，保留扩展目录；开关使用独立 JSON 状态，不改变已有数据库或设置格式。 |
| 插件实时能力 | `d0365381`、`17f94500`、`c9e2c2ec`、`d26867e9` | 接入权限控制的全局快捷键与 WebSocket，补充不支持音频时的明确错误，处理 ws 可选原生依赖打包。保留域名白名单、资源释放及权限撤销边界；仅增加 ws 依赖，不替换 fork 打包身份。 |
| 自定义智能体与插件语言 | `10527245`、`3c9bae9f`、`0fd58300`、`e4ffbfe4` | 可信扩展可注册完整智能体实现，插件行展示智能体名称并随应用语言更新。保留指定模型与 Google fetch 兼容；切换时按当前提供商取凭据，激活失败清理实例，避免复用未激活缓存。移除冲突中夹带的插件提供商数据库特性测试。 |
| 相关回归用例源码 | `e8f948eb`、`5c26aeee`、`ab180b65`、`796fb5f2` | 同步工具文件跳转、方形图标、Windows 项目路径比较、插件语言切换的测试源码；本轮没有执行。 |
| 插件提供商归属（后续确认） | `fe367677`、`5c09cee1`、`d26fe215`、`eb0b4026`、`8292a64e`、`422a41f3`、`d4b7bd3d` | 合入 schema 17 所属字段、插件配置同步、密钥入口、只读配置及卸载清理。保留手动配置导入导出，插件托管项不参与手动配置匹配。 |
| 持久化优先队列（后续确认） | `681902f9`、`411ea691`、`06d1a200` | 合入 schema 18 优先顺序、移动/编辑/锁定和停止后的出队修复。“立即发送”改用上游平稳停止当前轮、优先组按点击顺序进入下一轮的实现，替换 fork 的旧队列引导通路及配套用例。 |
| 完整插件模型场景源码（后续确认） | `e00081b3` | 同步此前因 schema 17 暂缓的完整场景，覆盖插件声明提供商与自定义智能体；未运行。 |

## 继续保留的边界

- 引用、批注、侧边对话的整组删除（`db001777`）：保留本 fork 现有功能。
- 上游 AGENTS/CLAUDE 镜像、强制 spec/ADR/E2E 流程、发布 CI：继续遵守本 fork 的 `AGENTS.md`。

本轮所有合并都必须保留：中文/英文品牌、小恐龙图标、独立安装身份、日期版本、
fork 更新源、共用 `.pi-desktop`、数据库 schema 18 和 protocol 11、内置终端修复。

## 已明确不采用的配套整理

- `e9852fe3` 的整组无用语言键清理依赖上游删除功能，不整体套用。
- `1ff571dc`、`ea7effa5` 仅做 Rust 格式化和测试模块搬迁，不是功能依赖；保留本 fork 含外部技能目录用例的结构。
- 上游文档重编号、流程约束和完整性脚本不属于本轮同步范围。

## 第一轮收尾

- 代码以固定快照逐个补丁适配，不进行整仓覆盖或整包 merge。来源逐项列在上表，后续同步应以本表判断已吸收内容。
- 提交前阅读了修改及冲突处理结果；业务数据库文件与迁移代码未改变，shared protocol 仍为 11，schema 仍为 16。
- 构建身份及版本配置仅在整理提交中修改；本轮上游补丁只给 desktop package 增加 ws 和类型依赖。
- 未执行测试、构建、typecheck 或应用启动，尚不能据此声称运行验证通过。

## 后续确认：数据库与队列

用户明确授权两项功能及数据库升级，并要求“立即发送”采用上游最新行为。重新通过 Git 查询并获取的上游 main 为 `42153b2f7cefde98d3a77df04f0bb3eab5489456`；相对第一轮快照新增的是代理认证修复 `10c1d446`，这两项功能没有额外变动，该网络补丁不属于当时两项适配范围，现已在下方正式合并中纳入。

- 数据库按原迁移链 16 → 17 → 18 升级，每步先做数据库备份并使用事务；旧手动提供商保持无插件归属，旧队列消息的优先级为 NULL。同步修正 shared 的 schema 常量为 18，protocol 维持 11。
- 新代码首次打开现有业务库时执行迁移；此次源码编辑没有启动程序或操作用户实际数据库。旧 schema 16 程序会拒绝打开升级后的数据库，回退须使用升级前备份，并单独保留升级后的新增数据。
- 立即发送先持久化优先顺序，再请求当前轮平稳停止。后续优先项尝试连续注入新启动的一轮，失败项保留队列；普通待发送消息可上下移动、取回编辑，优先组在界面锁定。
- 移除 fork 旧队列引导接口及等待唤醒改动；普通 Alt+Enter 引导沿用上游原有实现。保留引用、批注、侧边对话的消息适配和其余品牌/模型定制。
- 保留持久化队列写满时的显式报错，以及持久化删除失败时保留内存队列记录的保护。
- 提供商手动导出排除插件托管项，导入只匹配手动配置；新插件密钥表单按 Enter 时也要求非空且不忙碌，避免空输入误清除已有密钥。清除密钥使用专门按钮。
- 同步上游相关用例，并补充旧版 16/17 数据升级后保留配置、会话及排队内容的用例源码。按用户约定未执行测试、构建、typecheck 或应用启动。

## 正式合并与同步起点

按照用户要求，先推送已有提交，再获取并固定上游 main 为 `19dafacef71abdef84649b05b7a9926ba6383022`，在当前 main 发起正常合并。本次目标范围已逐项归入采纳或明确排除，未用 ours 策略或伪造提交父关系代替代码适配。

新增采纳内容：

- `10c1d446`：Chromium 对带认证代理使用应用内的回环转发，Node/curl 保留原代理认证；沿用已有出站地址及路由安全边界。
- `6b591ecd`：模型上下文大小增加可选来源标记，目录修正可传到已保存配置，手动修改的值优先；不再升级 SQLite schema。适配时修正旧的非默认手动值被误标成目录值的问题，并让设置提示遵守同一判断。导入导出和复制配置保留来源信息。
- 上游分支合并中的窗口控制间距、macOS 红绿灯占位修正，以及对应的问答卡片、折叠阅读位置和运行状态显示场景源码。
- 为新增来源字段补齐本 fork 的 Rust 测试数据，并增加旧手动值经保存、再次读取后不被目录覆盖的用例源码。

明确保留本 fork 的引用、批注、侧边对话及全部产品身份配置；维持归档文档、现行 AGENTS 规则和现有 Rust 测试模块布局。依赖锁中自动合并产生的重复 ws 类型记录已去重，先前保留的 Google fetch、密钥表单和持久化失败保护继续有效。

本次仅做源码合并、冲突处理和提交关系核对，未运行测试、构建、typecheck 或应用服务，也未操作用户实际数据库。

## 后续决议：恢复 fork 的“立即发送”当前轮引导（2026-09-17）

用户明确要求恢复 fork 原实现：官方“即将发送”在长时间运行的轮里体验差。本次恢复当前轮引导，保留上方记录的正式合并关系与同步位置；相关上游队列行为按下述决议适配。

- 恢复 `6820b56ef883281388d86f78196af1d95448d5d4` 的语义（被 `d18b7f3b757868113bd624a2af159ffbb32d51ea` 的优先队列适配替换）：运行中“立即发送”把该条持久队列消息投递进当前轮（durable queued steering），不调用 stop、不等轮结束；空闲时沿现有优先队列由下一轮发送。未做整提交 revert/cherry-pick，按当前代码逐层适配该方法。
- 去掉队列记录前先取得持久化回执（复用事件持久化的同一个 message key），失败时保留队列并重试；active expectedTurnId 校验、重复请求幂等、投递结果未知时暂停该会话队列且不复用该输入的保护一并恢复。TaskWait 等待可被引导唤醒，未完成的子智能体继续运行。
- 保留上游合入的优先顺序持久化、`priority` 与上下移动规则作为兼容能力；优先组按顺序逐轮消费，不再后台自动合并注入。只有显式“立即发送”才投递当前轮。schema 18、protocol 11 与既有数据库、队列记录格式不变，未降级、未改迁移链。
- 队列转移增加独立的 `queued-steering-receipts.json` 回执文件：投递前保存 pending，确认接收后保存 accepted 和消息副本；accepted 重试只补持久化及移除队列，不再次注入。结果未知的 pending 在重启后仍保留隔离，不能作为普通队列自动重发。该文件是本 fork 的恢复辅助记录，旧版本不识别此保护。
- 同步更新 AGENTS.md 的“立即发送”条目；本轮未提交、未推送、未构建、未启动服务、未执行测试，仅按用户允许范围做静态 typecheck 检查。

## 本轮同步：官方新增能力与 fork 定制适配

目标为 `5f0f38d768cd9d4ddd75a459a9dd7e7b8543a991`，从上一正式合入节点 `19dafacef71abdef84649b05b7a9926ba6383022` 增量同步，合并前本地提交为 `9775d52c390d42908abf8104975ad04cc81e58e0`。区间包含 120 个提交，其中 68 个非合并提交。用户先要求评估，再确认按建议适配，并明确接受官方插件市场设备标识与窗口快捷键调整。

本轮采纳范围：

- 上下文压缩摘要重试、预算估算与超限裁剪、摘要降级状态；可信完成通知首条回复可静默结束，用户引导到达后撤销该豁免。
- NDJSON 按 LF 分帧的 Unicode 修复，覆盖 host、sidecar、插件进程和 Codex 历史导入；sidecar 的 ESM 包声明修复。
- host 跨会话消息 ID 隔离、孤立历史恢复和重复 JSONL 残留修复；outbox 写满前尝试刷新，但保留 fork 的写满报错和真实持久化成功回执。
- 官方插件渠道、GitHub/CNB 备用渠道、动态镜像解析、安装进度与取消。用户明确接受官方下载解析请求发送稳定设备标识摘要、插件 ID 和版本；不发送原始机器标识，保留网络与校验边界。
- 开发插件权限审查与权限扩大确认、透明浮动小组件、插件界面获取宿主语言、麦克风权限处理。
- 宿主语音转录/合成、语音设置和插件接口；遵循目标上游的最终界面，不恢复其已移除的输入框语音按钮。
- 窗口显隐合并为 `toggleWindow`，默认 `Alt+Shift+W`，迁移旧唤出/关闭快捷键配置。
- 聊天宽度拖动、紧凑思考显示、窄输入栏、弹窗遮罩、侧栏样式与动画、项目 Git 克隆和二次点击删除；保留运行项目删除前的停止确认。
- 技能市场 fake-IP 分类与诊断显示、相关用例源码及其他配套功能接线。

继续保留或明确排除：

- 子代理继续使用 fork 的 `TaskResume`、`expectedExecution` 和加密快照跨重启召回。上游 `Task.resume`、`delegation-chain`、`delegation-history` 及其专属用例不叠加引入，后续同步按此边界适配。
- 任务级过程抽屉、耗时/token 统计、文件审查卡、停止/失败终态和当前轮引导不被上游单轮过程展示替换；紧凑思考显示与旧历史展示在现有任务结构上适配。
- outbox 不采用“捕获 UNIQUE 错误后直接视为成功并出队”的行为；此错误不能代替 fork 的持久化回执，修复由 host 的消息隔离承担。
- 保留品牌、图标、独立安装/缓存身份、共用业务目录与启动互斥、日期版本、fork 更新源、schema 18 / protocol 11、模型绑定、Google fetch 兼容、分区字体字重与配色、模型配置导入导出、外部技能目录及内置终端。
- 保留引用、批注、侧边对话；仅维护简中和英文，不恢复上游其他语种及其更新日志。
- 适配审阅补充：outbox 入队与会话清理串行处理，避免写满刷新期间旧快照晚到覆盖新快照；插件权限与面板打开流程按实际批准权限和窗口生命周期适配。
- 上游规则镜像、根 README、流程/spec/ADR 文档更新与重编号不引入；既有 `docs/archive/` 保留原归档快照。本记录是当前适配决议来源。

按用户要求只做代码合并与源码审阅，未运行测试、typecheck、构建、应用服务或浏览器预览，未操作用户实际数据库，未调用语音服务或插件下载解析接口。

## 本轮增量：远程主机、导入工作台与提示词增强

本轮固定上游目标为 `1a167af0056ff69ac17378c6c123d12b2c874daf`，从 `5f0f38d768cd9d4ddd75a459a9dd7e7b8543a991` 增量同步，区间共 120 个提交、72 个非合并提交，上游版本为 `0.15.1-beta.6`。合并前本地 HEAD 为 `b0f341b91d43e91efec3d079d8d99402b8b22a16`，继续在 `main` 使用正常 `git merge --no-ff --no-commit`。

用户本轮确认：

- 采用上游语音设置页调整，保留底层语音服务。
- 插件设置完全采用 `scenicThemes` 宿主主题卡片，允许旧 HTML settings destination 接口失效。
- 接入远程主机、RACP 与 SSH 密钥/密码配对安装，适配本 fork 的 `pi-host` 打包入口；本轮不构建、不发布。

采纳内容与适配范围：

- 共享 `host-runtime`、无界面 `pi-host`、RACP WebSocket、远程主机注册和连接生命周期、SSH 安装与隧道、加密凭据保存。保留本地当前轮引导、持久回执、子代理快照恢复及执行版本校验。
- 技能/MCP 扫描与批量导入、分类导入工作台、MCP OAuth；Pi 提供商优先从原生配置导入，避免 CC Switch 旧快照丢失模型。保留手动模型配置导入导出和插件托管配置隔离。
- 提示词增强支持用户模板、独立模型与思考强度，改善默认改写提示并在超时后取消请求。
- 插件主题卡片和包内主题资源、插件只读用量接口、CommonJS 导入包装迁移、完整 MCP 工具目录发现。
- 双击恢复工作面板宽度、历史消息滚动定位、显式打开文件审阅面板、链接打开目标、多行 TeX 公式、窗口边界及弱模型路径参数别名修复。
- 原生联网搜索入口在该上游区间中先加入再撤回，按目标提交的最终状态处理。

本轮额外适配：

- 符号链接导入扫描为 `linked-import`，禁止编辑和移动，解除导入只删除链接本身；外部扩展目录继续为 `linked` 且只读。同步扩展导入用例源码，未执行。
- 单文件夹项目组删除在运行中会话检查及项目删除完成后才清理组记录，避免拒绝删除时先丢失组信息。
- 共享运行时迁移后继续通过桌面侧扩展处理 `subagent.persistence`，保持 TaskResume 的会话绑定、加密快照和关闭清理；当前轮引导仍以持久化回执为完成条件。
- 上游单轮 `permissionMode` 参数尚未作用到 host-core，保留 fork 对单轮权限与会话权限不一致请求的拒绝行为。远程上限与会话模式不一致时不能发起该轮；远程当前轮引导按能力不可用返回，不转投本地处理。
- `pnpm build:pi-host` 统一经过 `scripts/build.mjs`，生成同日期版本的 `pi-host-<内部版本>-linux-x64.tar.gz` 和 `.sha256`，按成功批次保留产物。Linux x64 原生构建同步编译 host-core；其他平台需显式提供目标主机二进制，跨平台终端依赖也须单独提供。下载仅指向本 fork；没有同版本发布产物时，SSH 一键安装不能完整使用。
- 侧栏继续使用 fork 固定 275px、仅折叠/展开的实现；上游可变宽度拖动及其专属辅助模块不合入，右侧工作面板的双击恢复宽度保留。

继续保留品牌、图标、日期版本、独立安装/缓存身份、共用业务目录与启动互斥、schema 18 / protocol 11、模型绑定、Google fetch、分区字体字重与配色、任务级过程抽屉、引用/批注/侧边对话和局域网远控插件。只维护简中与英文。上游流程文档、规则镜像、发布 CI 及官方 macOS 签名发布配套不纳入；macOS 继续使用 fork 现有手动更新方式，保留既有归档快照。

本轮以源码阅读和差异审阅为主；打包子任务曾执行配置文件解析和脚本语法检查，随后补修仅做源码复核。未运行测试、typecheck、完整构建、依赖安装、应用服务、浏览器预览或实际远程部署，未操作用户实际数据库，也未发布或推送。同步前已有 11 个构建版本文件单独保留，不并入本次合并提交。

## 本轮增量：原生搜索、思考档位与交互改进

从 `1a167af0056ff69ac17378c6c123d12b2c874daf` 同步到 `ecece8f570fc0c18f21e4941eafe52244a7c6e5f`（上游 `0.15.1`），包含 106 个提交、68 个非合并提交。合并前工作区干净，在当前 `main` 正常合并，保留两个父提交。

用户确认采用 schema 19、移除内置字体，远程主机入口不受开发者模式限制；其余按评估建议适配。

采纳内容：

- 模型原生联网搜索：Responses / Anthropic Messages 按模型显式启用，适配器传递搜索工具、流式活动、来源与重启后的搜索记录回放；默认关闭。依赖补丁及锁文件同步更新。
- 会话及模型默认思考级别支持 `omit`，不覆盖提供商默认行为；思考滑条合并连续提交，模型固定保留当前思考级别。数据库沿上游 18 → 19 迁移，先备份再重建会话表；shared 与 Rust schema 均为 19，protocol 保持 11。旧 schema 18 安装包无法打开升级后的共用数据库，回退须使用升级前备份。
- 已选模型与提供商卡片拖动排序，提示词增强设置集中到 AI 卡片，项目分组卡片与详情管理。
- 图片发送前预览、消息与对话右键菜单、文件引用核实与原生删除修复、详细过程展示、窗口按钮层级、托盘会话快捷入口。
- Plan / Goal 产物优先使用内置文件视图；启动恢复先取得插件视图列表。
- Codex 导入按路径倒序扫描并显示 250 文件上限，SSH 安装诊断与密码登录修复。
- 移除四种内置字体及字体资源声明，继续支持系统字体、浅深配色下的分区字体和字重设置；远程主机仍在设置导航和搜索中显示。

适配与保留：

- 保留模型绑定、独立压缩模型、Google fetch 兼容、TaskResume 执行版本及加密快照恢复；聊天任务投影与原生搜索数据共同持久化。
- “立即发送”继续持久化投递当前轮，不停止或等待轮结束；outbox 对重复 ID 或明确来源拒绝保留消息、延后处理，后续消息可继续写入，不生成虚假成功回执。暂时性错误仍暂停刷新。
- 保留任务级过程抽屉、耗时/token 统计、文件审查卡、引用、批注及侧边对话；右键菜单遵循只读/运行状态，复制对话过滤内部批注附带内容。
- 补修滑条快速拖回原档位的提交遗漏，后台固定模型同步保存思考级别；相关现有用例源码随 schema、字体和入口行为调整。
- 保留品牌、恐龙图标、日期版本、fork 发布源、独立缓存与共用业务目录互斥、固定 275px 侧栏、内置终端、模型导入导出和插件托管配置隔离。
- 上游开发版业务目录隔离、官方 macOS DMG 签名配套、其他语种及上游流程/归档文档更新不引入；沿用已有安装身份和归档快照。

本轮仅做源码适配、差异审阅和 Git 合并关系核对，未运行测试、typecheck、构建、依赖安装、应用服务或浏览器预览，未操作实际用户数据库，未发布或推送。处理期间出现的局域网远控插件改动属于其他工作，保留在工作区，不纳入本次同步提交。

## Computer Use（Win 版）插件专项同步

来源为独立插件仓库 `https://github.com/TheFlareStar-Re/PI-Computer-Use`，目标版本 `0.4.0`，固定提交 `fba546d213a48ef32983cd75d9d9b85f212b0e85`。以作者的 `1432fbd`（0.3.1）对照本地 Windows 定制版 0.3.4，按差异适配；本节独立记录插件来源，不改变上方 PI-Desktop 主仓库的正式合入位置。

同步范围：Office 桌面操作与工作流技能、PowerShell 7 优先选择及 Windows PowerShell 回退、焦点和快捷键处理、保存对话框与中文文件名读取，以及对应上游测试源码。

保留本地 Windows 离线运行包、驱动用户目录环境补齐、自动启动、修复并启动、`cu.indicator` 侧栏状态接口、允许列表和急停。上游此次未更新驱动，继续使用 `vendor.json` 记录的 cua-driver 0.28.2 及现有校验值。

用户确认采用上游 `perform_secondary_action` 修复：仅路由 `Invoke`、`SetValue` 和明确方向的滚动；其他动作返回未发送错误，不再自动映射为点击或默认向下滚动，避免选择操作误打开文件。

本次仅适配源码；未运行测试、构建或启动插件，未替换应用中已安装的插件，未提交或推送。

## 本轮增量：定时任务、历史存储与稳定性修复

从 `ecece8f570fc0c18f21e4941eafe52244a7c6e5f` 同步到 `8e3b2484a9ae000ad75ec10a2ffb6fd9a07221a4`（上游 `0.15.2-beta.1`），共 59 个提交、38 个非合并提交。合并前工作区干净，在当前 `main` 正式合并，合并提交为 `99ab74e3f9e42a606c1af17482fea06e60e6d3b9`。

用户在评估后确认吸收，并明确同意数据结构采用上游实现。

采纳内容：

- 定时任务支持每小时、每天、每周多选日期、手动运行、编辑、暂停及最近 100 次运行记录；Agent 工具可管理当前会话项目的任务。桌面运行期间轮询调度，自动任务采用 Ask 权限，不补跑错过时段，不重叠运行，使用保存的项目和执行时默认模型。
- 定时配置沿用现有 `config_json`，扩展 `schedule`、`nextRunAt`、`workspacePath`，旧的仅周期任务保持未启用状态，待用户明确保存周期后启用。
- 历史分支使用 `revision_live` 引用当前对话后缀，切换、重新生成或删除根消息前先完整归档，减少重复存储。新代码兼容旧 `revision` 记录；旧版本不识别新引用记录。按用户授权采用此结构，schema 19 / protocol 11 保持不变。
- 模型流连续 180 秒无事件时取消请求并进入既有重试流程；结合中文输入估算限制主代理和子代理的输出预算，覆盖 `omit` 请求路径。桌面模型进程使用系统受信任证书，证书验证错误明确报错且不重试，保持 TLS 校验。
- 插件/MCP 各层超时预算对齐；插件子进程获得用户目录环境变量，MCP 使用登录 shell PATH；npm 缺失可选择可执行文件并保存，打包模型进程启用 `PI_BUNDLED_NODE`。
- Windows 会话文件原子替换与商店版 PowerShell 识别、原生文件选择器防重复、聊天选区复制、详细模式自动展开最新工具、持续运行提示、长错误提示及弹窗边界、设置帮助图标、模型目录更新。
- 本地崩溃采集与下次启动诊断，保持不上传；补充本 fork 简中与英文更新说明。

适配与保留：

- 崩溃文件和报告标记统一放在独立 Electron `userData/diagnostics`，在取得业务目录互斥锁后初始化；保留原有真实路径锁键和默认原版 profile 互操作锁。
- 保留 TaskResume、执行版本校验、普通 JSON 快照及旧加密快照读取、安全边界完整报告投递、当前轮引导与持久回执；不引入上游 `Task.resume` 和转录恢复通路。
- 保留 Google fetch 兼容、模型绑定和独立压缩模型、任务级过程抽屉、统计、文件审查卡、引用、批注、侧边对话、固定 275px 侧栏、分区字体字重及配色、模型导入导出、插件托管隔离、技能扩展目录、内置终端及局域网远控插件。Windows 插件环境继续传递 `PATHEXT`。
- 审阅补修：有项目归属的手动任务运行时使用保存的项目，改为自动周期时也保留该归属；Bash RPC 默认及显式超时预算计入宿主的 30 秒排队等待，命令执行时长不变。相关回归用例源码一并维护。
- 保留 fork 品牌、图标、日期版本、发布源、安装身份、独立缓存及共用业务目录。上游版本号、流程规则、归档文档更新、额外语种和 PR 截图不纳入；本轮目标的功能改动均已采纳或按上述边界适配，无待决定功能项。

本轮进行了源码审阅、冲突处理、暂存差异格式检查和 Git 父提交/祖先关系核对；未运行测试、typecheck、构建、依赖安装、应用服务、浏览器预览或实际定时执行，未操作用户实际数据库，未发布或推送。

## 本轮增量：子代理上下文、多级折叠与模型重试

本轮固定上游目标为 `f6dd5e28479c040bfcaa6dcb31305604dc1e4936`，从 `8e3b2484a9ae000ad75ec10a2ffb6fd9a07221a4` 增量同步，共 59 个提交、35 个非合并提交，上游版本为 `0.15.2-beta.2`。合并前本地 HEAD 为 `dbcf42410`，在当前 `main` 使用正常 `git merge --no-ff --no-commit`。

用户在评估后明确同意采用上游模型目录上下文上限，并吸收其余功能；保留既有 fork 定制。

采纳范围：

- 主代理与子代理共用上下文预算公式；子代理在请求边界自动压缩，失败后缩减历史，仍无法容纳任务时返回 `SUBAGENT_CONTEXT_OVERFLOW`。模型目录上限参与输出预算和溢出判断。
- Pi 核心依赖升级到 `0.86.1`，适配系统消息与 Agent 接口，更新原生搜索及推理回放补丁，并启用 Meta OAuth。
- AI 设置新增可选无限重试，默认关闭；保留错误分类、退避和用户停止。桌面及无界面宿主传递设置，主代理和子代理按设置执行。
- 支持项目 `.pi/` 与全局 `~/.pi/agent/` 中的 `SYSTEM.md`、`APPEND_SYSTEM.md`，按项目优先解析；角色描述替换不移除工具、协作和委派运行规则。
- 思考与执行过程多级折叠、失败统计、搜索展开、会话内折叠选择保持；工具宽度跟随会话区域，标题按可用宽度省略。
- 思考滑条采用独立组件、轨道刻度对齐及动画，保留快速连续提交和回到原档位的保护；草稿保留工作区归属，异步粘贴保留原附件，保存提供商保留仍然存在的默认模型。
- 浏览器预览按会话与导航序号隔离异步结果和原生事件；MCP HTTP 202 确认响应正确处理；插件市场备用包显式使用管理器数据目录；单次模型请求补齐失败信息和输出预算。

适配与保留：

- 子代理使用 fork 的 `TaskResume`、执行版本校验与快照恢复；普通 JSON 快照兼容已有加密文件，保留模型绑定、当前轮引导和持久回执。上游 `Task.resume` 与转录重建恢复通路不引入。
- Pi 0.86.1 兼容补修：重建上下文与预算提醒使用系统消息序列，保留旧快照工具声明读取；子代理沿用每次请求的上下文转换入口，覆盖首次请求、恢复、重试和后续工具轮，降级后不以旧助手用量误判窗口。压缩结果仍通过现有快照保存。
- 保留任务级过程抽屉、耗时/token 统计、文件审查卡、引用/批注/侧边对话；普通历史消息使用上游过程折叠，任务过程仍使用 fork 抽屉。补齐搜索命中任务过程时的外层展开、思考行 Hook 导入及过程组件闭合标签。
- 保留独立压缩模型、Google fetch 兼容、模型导入导出、插件托管隔离、扩展技能目录、固定 275px 侧栏、分区字体字重与浅深配色、内置终端及局域网远控插件。
- 保留 fork 品牌、图标、日期版本、发布源、安装身份、独立缓存和共用业务目录；schema 19 / protocol 11 不变。新增重试设置为可选字段，不执行数据库迁移。
- 上游流程规则、发布检查脚本、额外语言、官方版本号及归档文档更新不纳入；维护简中与英文更新说明。同步前的 README 本地修改单独暂存，完成合并后恢复；未跟踪的 `release/` 不纳入提交。

本轮进行了源码适配、独立差异审阅、暂存差异格式检查及 Git 合并关系核对；为核实接口，仅下载 npm 官方 Pi 0.86.1 包中的源码到会话临时目录阅读，未执行包代码。未运行测试、typecheck、构建、依赖安装、服务或浏览器预览，未操作用户实际数据库，未发布或推送。

## 本轮增量：图片生成、运行时稳定性与模型绑定修复

本轮固定上游目标为 `0111e306c120ad5820688d7608cb37bad8fbcc1f`，从 `f6dd5e28479c040bfcaa6dcb31305604dc1e4936` 增量同步，共 91 个提交、50 个非合并提交，上游版本为 `0.15.2`。合并前本地 HEAD 为 `4cd0091be`，在当前 `main` 使用正常 `git merge --no-ff --no-commit`，合并提交为 `535ac0bc3`，两个父提交分别为 `4cd0091be` 与上述上游 SHA；已通过 `git merge-base --is-ancestor` 确认祖先关系。

用户评估后确认全部吸收，包括图片生成功能。

采纳范围：

- 聊天图片生成与编辑：设置中配置唯一生图模型（新增 `imageGeneration` 绑定），`GenerateImages` 工具权限与本地取消接线，批量生成，预览在 Chromium 解码验证，图片绑定从对话模型选择排除；新增内置 imagegen 技能与 `models.dev` 目录更新。
- Agent 运行时稳定性：上下文估算按实际请求成本校准并加固边界；工具调用 ID 每次请求唯一（请求前视图去重）；子代理工具调用去重；成功响应后重置重试预算；续跑恢复加固；失败流可留下多行助手消息时移除整个失败后缀。
- Composer 与聊天修复：工作区切换清 undo 历史、文件引用原生 undo 恢复、消息编辑草稿复制、URL 尾部标点与括号不进链接。
- 定时任务修复：每小时任务不再要求日历字段；手动任务以 `workspace_bound` 标记保留工作区绑定（取代 fork 此前按路径判断的补修）。
- 模型绑定与插件提供商修复：存储绑定逐条读取、degraded 更新保护（新增 `MODEL_BINDINGS_DEGRADED` 错误码）、插件提供商保留声明的 thinkingLevels。
- MCP 市场加固：注册表 header 变量与 URL 隔离、未绑定 token 保持字面量、router fake-ip 源识别（沿用 fork 的 `allowFakeIp` 代理判定）；落地页插件崩溃与提供商重试加固。
- 桌面杂项：项目名默认取主文件夹名、归档会话从项目打开恢复、插件崩溃诊断命名退出码、复制完整对话历史、便携版固定解压路径（`unpackDirName` 采用 fork 身份 `this-is-a-agent-Portable`）、CSS 优化警告清理、设置往返保留无限重试布尔值。
- 按既有边界排除：上游版本号 0.15.2、GitHub CI、Vercel/docs 部署、docs 站点依赖、其他语种及更新日志、上游流程文档；`docs/archive/` 保持原归档快照，新增的上游归档文档不引入。

适配与保留：

- 子代理保留 fork 的每次请求上下文转换入口（`transformContext`/`prepareRequestContext`），不切换回上游本轮的回合边界保护；同时采纳上游的工具调用去重（`dedupeToolCalls`）与失败后缀清理，重试清尾仍经 fork 的 `setAgentMessages`。
- 工具取消保持 fork 的全工具调用 ID 取消语义（所有工具注册取消接收器），未采用上游仅限 Bash/GenerateImages 的收窄；`getApiKey` 维持 fork 的 `providerRequestKey` 直取。
- 定时任务取上游 `workspace_bound` 实现，fork 的旧路径判断补修由该字段取代。
- 保留品牌、恐龙图标、日期版本（各 package/Cargo 版本字段维持 `2609.2121.5541`）、发布源、安装身份、独立缓存与共用业务目录互斥、schema 19 / protocol 11、模型绑定、Google fetch 兼容、分区字体字重与浅深配色、任务级过程抽屉、引用/批注/侧边对话、固定 275px 侧栏、内置终端及局域网远控插件；仅维护简中与英文。
- 设置新增 `imageGeneration` 与 `infiniteProviderRetry` 均为可选字段，不执行数据库迁移。

本轮进行了冲突处理、源码审阅与 Git 父提交/祖先关系核对；未运行测试、typecheck、构建、依赖安装、应用服务或浏览器预览，未操作用户实际数据库，未发布。推送待用户确认。

## 本轮增量：多生图模型、定时任务与附件修复（2026-09-22）

从 `0111e306c120ad5820688d7608cb37bad8fbcc1f` 同步到 `206085c07697771f857cab0421bc145e3af41e1d`，上游版本仍为 `0.15.2`，共 65 个提交、25 个非合并提交。用户先审阅增量，再确认按建议吸收适配。合并前工作区干净，在当前 `main` 正式合并。

采纳范围：

- 多生图模型候选与默认项切换；新增提供商保留可运行默认模型，手输模型 ID 从本地目录补全能力与限制，异步结果不覆盖已编辑行。
- 定时任务到期并行派发且同任务防重复；删除项目暂停绑定任务并保留定义和历史；规范化工作区路径；旧未启用任务可维护，周期切换保留用户明确设置的日历时间。
- 临时会话附件预览、分叉会话复制被引用的粘贴附件并重写引用，失败回滚；目录别名与真实路径均在真实路径授权边界内读取。
- 排队消息保存期间锁定操作并解释状态；中文输入法 Esc、项目编辑草稿、快捷键重置冲突、弹层层级与浏览器页内导航修复。
- 插件声明的思考档位字段类型校验；维护对应上游测试源码及简中、英文应用更新记录。

适配与保留：

- 排队消息保持 fork 当前轮引导、持久化回执、请求中锁定与优先队列规则；不改成停止当前轮后发送。已有 pending 删除/编辑保护继续保留。
- 保留模型配置导入导出和插件托管隔离、独立压缩模型；压缩模型选择及运行时排除生图候选，过期绑定沿用主模型回退。修正模型配置页已有多余闭合括号。
- 保留 TaskResume、执行版本和普通 JSON 快照兼容恢复、子代理观测窗口与插件化观测条、任务抽屉、统计、文件审查、引用/批注/侧边对话、Google fetch、固定侧栏、分区字体字重与配色、扩展技能、终端及局域网远控插件。
- 保留品牌、图标、日期版本、fork 更新源、独立安装缓存和共用业务目录互斥；schema 19 / protocol 11 不变。`imageGenerationModels` 与 `config_json.calendarConfigured` 为兼容读取旧记录的可选扩展，旧版本不理解新增字段完整语义。
- 按既定边界保留现有 AGENTS 规则和归档快照，不引入上游流程规则、规则检查脚本及其他语种；本轮不涉及智能体提示词规则修改。

本轮仅源码适配、差异审阅和 Git 合并关系核对；未运行测试、typecheck、构建、依赖安装或服务，未操作实际业务数据库，未发布或推送。
