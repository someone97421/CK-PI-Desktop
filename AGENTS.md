# AGENTS.md

这是 `someone97421/CK-PI-Desktop` —— PI-Desktop 的**个人 fork**，自用魔改，纯 vibe。
不是给原项目（`vastsa/PI-Desktop`）做贡献的地方。

上游那套 spec / ADR / 交付流程文档已整体归档在 `docs/archive/`，只当字典，不用遵守。

## 底线（只有这 7 条）

1. **不往上游去。** 不给原仓库或任何其他上游仓库提 issue / PR / review，不新增
   `upstream` 远端，不往本 fork 以外推。`origin` 就是本 fork。
2. **不泄密。** API key、token、密码、凭据、私人数据不进仓库；提交前扫一眼 diff。
3. **别把数据搞坏。** 动数据库或持久化格式时，要么兼容旧数据，要么说清楚怎么迁移。
4. **别自己削弱安全。** 不为了让功能跑通去关权限检查、绕过沙箱、放宽 URL/路径校验。
5. **提交信息用中文。** 格式随意，`type(scope): 说明` 就行，别写 `update` 这种废话。
6. **如实说。** 没跑的测试就说没跑，别把"应该没问题"说成"验证过了"。
7. **大破坏先问一句。** 删掉还在用的东西、改公共接口、改动数据格式之前先确认一下。

## 其余全自由

- 不需要写 spec、ADR、E2E 文档、change checklist。
- 不需要开分支或 worktree，直接改。
- 不需要维护进度表（`docs/PROGRESS.md` 和 `docs/DECISIONS.md` 留着，用不用随你）。
- 测试、构建、typecheck 想跑就跑，不想跑就不跑；跑了就如实报结果。
- 想起一出是一出没问题。

**Harness 规则维护**：修改提示词、工具说明或智能体行为规则时，优先删除、改写、合并已有表述，以简洁的通用原则表达意图。只有现有规则无法清楚承载必要语义时才新增；禁止用罗列场景、例外或禁令的方式堆叠规则，避免将单次失误逐条固化为补丁。优化规则时先输出目标改动的 diff 供用户确认，确认后再落实到文件。

## 局域网远程插件授权与产物（用户确认，2026-09-17）

- **局域网远控授权（用户追加确认）**：`local.lan-remote-control` 以主机密码登录作为用户授权。登录后可直接修改模型、思考级别、工作模式和权限模式，提交工具审批、智能体回答及 Plan/Goal 决议；宿主直接执行，不再要求电脑二次确认。此规则覆盖该插件的全部宿主操作。
- `plugins/lan-remote-control/dist/*.piplug` 体积较小，随对应源码一起提交、推送到本 fork。
- 后续修改此插件并提交推送时，先用 `npm --prefix plugins/lan-remote-control run pack` 构建，再提交当前源码对应的插件包；不把历史旧包顺带补入提交。
- 插件的 `node_modules/`、`.build/`、运行数据及认证文件不进 Git；插件构建不代表已完成宿主构建或手机实机验证。

## 构建产物保留（用户确认，2026-09-17）

- 插件和主程序的产物存放目录只保留最近一次成功构建的产物。新版生成成功后自动删除历史包、旧解包目录及历史构建暂存目录；构建失败时保留上一份成功产物。
- 主程序同一次构建生成的安装包、便携版、解包目录、更新描述文件和 blockmap 作为一整套保留，不按单个文件的新旧时间删选。`scripts/build.mjs` 在输出根目录下创建独立批次目录，成功后清理旧批次。
- `THIS_IS_A_AGENT_OUTPUT_DIR` 指定产物输出根目录；使用它时同样自动清理其中的历史产物。目录内与构建无关的文件不得误删，不得越过产物目录清理源码、依赖或用户数据。
- 插件 `dist/` 只保留本次 `.piplug`；`.build/` 只保留最近一次成功生成的 `plugin-*` 目录及共用工具。已跟踪的历史插件包需要随新版一起提交删除。

## 产品身份与隔离边界（用户确认，2026-09-16）

后续修改必须遵守本节；不可把品牌替换扩散到数据格式或兼容协议。

### 名称、图标与安装

- 中文名固定为 **这是一个助手**，英文名及可执行程序名为 **this-is-a-agent**。
- 身份配置源为根目录 `app-branding.json`。独立应用 ID 为
  `com.someone97421.this-is-a-agent`，开发版系统身份使用 `.dev` 后缀。
- 根目录 `ico.png` 是唯一图标源。用 `python scripts/make-icon.py` 派生
  Windows ICO、macOS ICNS、Linux PNG、托盘图标和界面品牌图片；不要独立修改派生图片。
- 安装包、卸载项、快捷方式、macOS bundle 和 Linux 包/desktop entry 使用本 fork 的身份。
  `build.extraMetadata.name` 固定为英文名，确保打包后应用名和更新下载缓存也独立。
- 软件更新和反馈只指向 `someone97421/CK-PI-Desktop`；插件市场及其他第三方资源地址不是软件更新源，不做盲目替换。
- 界面只支持简体中文（`zh-CN`）和 English（`en`），保留跟随系统选项。语言包、更新日志及安装包语言资源均按此范围维护；同步上游时不恢复其他语言。旧配置中的中文变体回退到简中，其他语言回退到 English。
- `kill-dev.cmd` 只能结束当前工作区的开发进程及其子进程，不得按全局端口、`*host-core*` 或原版程序名批量结束进程。

### 明确共用的业务数据

- **继续共用 `~/.pi-desktop`**。对话、项目、配置、凭据、插件状态及业务日志仍在此目录；不改目录、不清空。结构迁移遵守下方用户确认的升级边界。
- 保留 `PI_DESKTOP_DATA_DIR`，用于显式指定独立业务目录。保留数据库文件名、插件 ID、兼容的 IPC/API、内部 `@pi-desktop/*` 包名和 Rust host 二进制名。
- 应用日期版本和数据库版本是两回事，不能为了重新计版去重置或提升 schema/protocol。
- 数据库、持久化设置和凭据格式必须保持原版兼容；无法保持时先向用户说明并确认。用户已确认采用上游 `revision_live` 历史分支引用和定时任务 `config_json` 扩展：新代码兼容旧记录，旧版本不识别历史引用；schema 19 / protocol 11 保持不变。
- 内置插件资产跟随各自的应用安装包，业务目录只共用插件状态/设置。两个版本交替运行可能各自重新登记内置插件；不要把共用状态误认为共用安装资源。
- 卸载程序不得删除这份共用业务数据。不要把用户数据、凭据或运行缓存提交到仓库。

### 独立的缓存与共用目录互斥

- Electron `userData` 使用系统应用数据目录下的 `this-is-a-agent`，Chromium 数据在其 `chromium` 子目录；崩溃报告和 Electron 日志也使用该独立目录。
- 同一真实业务目录只允许一个桌面进程启动。默认目录在取得 Electron 单实例锁时临时沿用原版的 `PI-Desktop` 名称和 profile 路径；取得锁后恢复本 fork 名称及独立缓存路径。
  这个锁路径是**有意保留的互操作边界**，不是漏改品牌。不要重构成仅按新 appId 加锁，否则原版无法识别占用。
- 自定义数据目录按归一化真实路径生成锁键；同目录开发版和安装版互斥，不同目录可以并行。
- 互斥必须发生在 Logger、outbox、数据库和插件启动之前；未取得锁的进程立即退出。
- 兼容锁针对原版默认 `PI-Desktop` profile。旧的改名副本、绕过单实例锁的旧版本、自定义目录的原版或直接运行旧 host 不会自动遵守新版规则；这些程序必须先退出，不得宣称能够拦住任意历史程序。

### 日期时间版本与构建入口

- 以北京时间 `Asia/Shanghai` 的构建开始时间计版，用户可见格式为 **YYYYMMDD-HHMMSS**，精确到秒。
- 为兼容 SemVer 和 Windows 16 位版本段，内部编码采用 **YYMM.DDHH.MMSS**，各段去除前导零。
  例如 `20260916-223045` 对应 `2609.1622.3045`，Windows 数字版本为 `2609.1622.3045.0`。
  内部版本用于更新排序和协议元信息；界面通过 `displayAppVersion` 显示完整日期时间。
- 支持年份为 2000–2099；不得把前导零数字直接写进 SemVer，也不得用不参与比较的 `+build` 单独承载时间。
- `scripts/prepare-build.mjs` 同步根/工作区 package 版本、Cargo 版本与本地包锁项、共享应用常量、安装包文件名。
  `packages/shared/src/app-build.ts` 是生成文件，禁止手工改其中常量。
- 正式构建、开发启动、打包统一走 `scripts/build.mjs`；一次构建树通过 `THIS_IS_A_AGENT_BUILD_TIME` 复用同一个 ISO 时间。
  多平台构建同一版本时须传入相同时间。每次构建生成的版本文件改动属于预期，不要自动提交或推送。
- 原项目的 `0.x` 更新历史保留作资料，应用内更新记录使用 `fork-changelog.ts`。新发布记录在本 fork 维护。
- GitHub 发布标签用内部 SemVer（例如 `v2609.1622.3045`），发布标题和安装包名显示完整日期时间。
  更新发布必须包含打包器生成的 `latest*.yml` 及对应安装文件；仅修改源码或推送 commit 不等于已发布更新。
- 默认不运行测试、全量构建或启动服务。按用户要求验证时只覆盖修改及相关部分，并如实说明验证范围。

## 上游同步边界

- 同步位置、已吸收改动与暂缓项记录在 `docs/UPSTREAM-SYNC.md`；按照下方固定流程合并，不将上游整文件覆盖本 fork。
- 保留模型绑定、Google fetch 兼容、分区字体/字重、浅深配色、模型配置导入导出、技能扩展路径和内置终端修复。模型上下文预算采用上游目录上限逻辑（2026-09-21 用户确认），目录值存在时，相关输出预算和溢出判断不得因手动填大窗口而越过该上限。字体仅使用系统已安装字体，按用户确认移除内置字体资源。
- 保留引用、批注和侧边对话；上游删除这些功能的补丁及依赖该删除的语言键清理，不自动同步。
- 扩展路径中的 linked 技能只读，移动功能须在界面与 host 两端拒绝这些技能。
- 用户已批准插件提供商、消息优先队列及 `omit` 思考档位，数据库沿上游迁移链 16 → 17 → 18 → 19 升级；升级前自动备份，保留旧数据，protocol 保持 11。只支持 schema 18 或更早版本的旧安装包不能打开升级后的共用数据库。
- “立即发送”恢复 fork 的当前轮引导语义（2026-09-17 用户决议，按 6820b56 恢复）：会话运行中把该条持久
  队列消息直接投递进当前轮，不停止、不等待轮结束；空闲时仍走现有优先队列，由下一轮发送。投递完成
  去掉队列记录必须以持久化回执为前提，投递失败保留消息。上游的优先顺序持久化、priority 与移动/编辑
  规则作为兼容能力保留；使用 schema 19 / protocol 11，队列持久化结构不变。
- 提供商导入导出只匹配用户管理的配置；插件托管项由插件声明恢复，导入文件不得覆盖其配置或凭据。
- 内置子智能体开关使用独立 `agent-capabilities/subagent-builtins.json`，不参与数据库迁移。
- 用户确认本轮继续跟随官方新增能力：采用官方插件市场、备用渠道、动态下载解析及设备标识摘要发送；设备标识仅随官方下载解析请求发送，保留现有网络与权限校验。
- 采用官方窗口显隐切换 `toggleWindow`，默认 `Alt+Shift+W`，按官方规则迁移旧唤出/关闭快捷键配置。
- 子代理恢复保留本 fork 的 `TaskResume`、执行版本校验和快照跨重启召回；快照以普通本地 JSON 保存，兼容读取已有加密文件，运行数据不进仓库。不叠加上游基于转录重建的 `Task.resume` 恢复通路。
- 聊天展示保留任务级过程抽屉、耗时/token 统计和文件审查卡；上游紧凑思考显示须在此基础上适配，保留引用、批注、侧边对话和当前轮引导。
- 侧栏保留固定 275px、仅折叠/展开的定制，不恢复可变宽度拖动或旧宽度偏好；右侧工作面板的宽度调整独立保留。
- 持久化 outbox 的成功回执不得由重复 ID 错误代替；吸收 host 的跨会话 ID 隔离及队列刷新修复，写满或写入失败仍保留明确失败语义。
- 本轮同步决议：采用上游语音设置界面调整，保留底层语音服务；插件设置使用 `scenicThemes` 宿主主题卡片接口，不维持旧 HTML settings destination 接口。
- 采用远程主机 / RACP / SSH 配对与安装能力，设置入口始终可见，不受开发者模式限制。`pi-host` 与桌面复用 fork 日期版本，打包入口统一经过 `scripts/build.mjs`，下载源只使用本 fork 发布；SSH 一键安装需要先发布同版本 Linux x64 主机包和 SHA-256 校验文件。源码同步本身不代表已构建或发布这些产物。
- 本地会话继续保留当前轮引导、持久化回执和 `TaskResume` 快照恢复；远程会话的能力按实际 RACP 支持展示，不把本地支持等同于远程支持。

### 固定同步流程

用户说“同步上游，保留我的定制”时，默认直接执行本流程，不让用户重新选择 Git 操作或重复列举已有保留项。

1. **先读边界与记录。** 阅读本文件及 `docs/UPSTREAM-SYNC.md`，检查当前分支和工作区；已有改动只在授权范围内整理提交，不覆盖或清理用户改动。默认在当前分支操作，不自行开分支或 worktree。
2. **获取并固定上游版本。** 通过 `git fetch https://github.com/vastsa/PI-Desktop.git main` 获取代码，记录本次目标的完整 SHA。保持只有本 fork 的 `origin`，不新增 `upstream` 远端。
3. **默认正式 merge。** 目标已是 HEAD 的祖先时，无须重复合并；否则长期同步使用 `git merge --no-ff --no-commit <目标SHA>`，适配完成后保留有两个父提交的合并提交。零散修复才使用 cherry-pick 或补丁；普通提交、squash 或文档记录不能代替合并关系，不重写既有历史。
4. **逐项处理差异。** 同时查看冲突和自动合并成功的改动，保留本文约定的产品定制；已授权采用上游的行为按上游适配。影响保留功能、数据兼容性或公共接口的新决定，集中说明后询问；已有明确授权不重复询问。
5. **如实收尾。** 提交前阅读实际 diff，确认冲突处理、敏感数据与定制边界。默认不运行测试、构建或启动服务；用户要求验证时只覆盖相关部分，并记录实际结果。只有目标范围的改动均已采纳或明确决定不采用，才完成合并提交；不得用 `git merge -s ours` 或伪造父提交来冒充已适配。
6. **更新同步位置。** 在 `docs/UPSTREAM-SYNC.md` 分别记录“最后正式合入的上游 SHA”“最后核对的上游 SHA”、对应本地合并提交及未合入项。完成后用提交父关系和 `git merge-base --is-ancestor <目标SHA> HEAD` 确认真实合并关系；仅核对过、挑过补丁的 SHA 不能记为正式合入。
7. **下次增量继续。** 以最后正式合入的上游节点为基础查看后续变化，同时复查记录中的未合入项；不重复搬运已适配补丁。若历史只有挑补丁记录，先核清已采纳与排除的改动，再建立一次真实合并关系。按本轮授权决定是否推送，目标只能是本 fork。

## 环境速查

```bash
pnpm install            # 首次需要，且会顺带修好 pnpm-lock.yaml
pnpm build             # 同一次构建时间下编译 JS 和 host
pnpm dev
pnpm --filter @pi-desktop/desktop dist:win
```
