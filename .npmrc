shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true

# Electron's own install script fetches its ~120 MB binary from GitHub
# releases by default, which fails on networks where GitHub is unreachable.
# The mirror below is used by @electron/get for every install/rebuild.
electron_mirror=https://npmmirror.com/mirrors/electron/
